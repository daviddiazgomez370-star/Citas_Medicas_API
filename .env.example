# Copia este archivo a .env solo para desarrollo local.
# En producción, configura estas variables en el entorno del proceso.
SECRET_KEY=replace_with_a_long_random_secret

# Opcional: usa una base SQLite diferente en desarrollo o pruebas.
# DATABASE_URL=sqlite:///./citas_medicas.db
